extends layout

block content
  h1= title
  p Number of Rows = #{query.rows} and Number of columns =  #{query.cols}
  - var rows = Number(query.rows) 
  - var cols = Number(query.cols)
  - if(!Number.isInteger(rows)) {
      p Given row value in not integer
  - }
  - else if(!Number.isInteger(Number(cols))) {
      p Given column value in not integer
  - }
  - else if (!(rows >= 1 && rows <= 10)) {
      p Given rows range should be in 1 to 10
  - }
  - else if (!(cols >= 1 && cols <= 10)) {
      p Given cols range shoulds be in 1 to 10
  - }
  - else {
        table#outer
            - for(let i = 0; i < rows; i++) {
                tr#middle 
                    - for(let j = 0; j < cols; j++) {
                        td= (1 + i + j * rows)
                    - }
            - }
  - }